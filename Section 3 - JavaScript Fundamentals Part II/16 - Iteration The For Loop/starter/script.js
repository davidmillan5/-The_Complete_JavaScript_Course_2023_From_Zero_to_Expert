'use strict';

// Loops are a fundamental process on programming

// The For Loop keeps running while conditions is TRUE

for (let rep = 1; rep <= 10; rep++) {
  console.log(`Lifting weights repetition ${rep} `);
}
